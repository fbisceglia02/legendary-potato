const app = Vue.createApp({
    // object representing the root component in vue
    // data, functions, component template

    // template ex.
    // template: '<h2>I am the template</h2>'
    // this template is in the #app component root, which is into the html div with id #app.
})

// take the app component and mount it in the dom
app.mount('#app')